# Copy to .env.local and paste your Google Apps Script Web App URL.
# .env.local is gitignored — never commit real URLs to the repo.
#
# After editing, run: node scripts/generate-config.mjs
GOOGLE_SHEET_WEB_APP_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
